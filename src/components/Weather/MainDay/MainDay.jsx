import React from 'react';
import './MainDay.scss';

const MainDay = (props) => {

    return (
        <div className={`${props.className} `}>

        </div>
    )
}
export default MainDay;